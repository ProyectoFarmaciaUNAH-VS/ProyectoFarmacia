@model ProyectoFarmacia.BL.Categoria

@{
    ViewBag.Title = "Detalle";
}

<h2>Categorías</h2>
<hr />
<div>
    <h4>Detalle del registro</h4>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Descripcion)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Descripcion)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Editar", "Edit", new { id = Model.Id }, new { @class = "btn btn-warning btn-sm", @type = "button" }) 
    @Html.ActionLink("Ir a la lista", "Index", null, new { @class = "btn btn-default btn-sm", @type = "button" })
</p>
